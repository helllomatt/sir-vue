import 'mocha';
import { expect } from 'chai';

describe('webpack', () => {
    it('should create a webpackbuilder instance', () => {

    });

    it('should apply the proper config', () => {

    });

    it('should write to the output folder', () => {

    });

    it('should compile the webpack config', () => {

    });

    it('should fail to compile the webpack config because of bad config', () => {

    });
});